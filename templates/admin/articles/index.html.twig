{% extends 'layout/layout.html.twig' %}
{% block title %}

{% endblock %}

{% block body %}
    
    <br><br><br>
    <div class="col d-flex justify-content-center text-center">
    <div class="card text-center " style="width: 40rem;">
  <img class="card-img-top" src="{{user.image}}" width="auto" height="100%" class="img-fluid rounded mx-auto d-block " alt="Card image cap">
  <div class="card-body" >
  <p>nom : {{user.nom}}</p>
  <p>prenom : {{user.prenom}}</p>
  <p>date de naissance : {{user.birthday|date('Y-m-d')}}</p>
    <p class="card-text">
    {{user.description}}</p>
  
{% if app.user is defined and app.user is not empty %}
                {% if is_granted('ROLE_ADMIN') == true %}
                    <a href="{{ path('app_admin', {'id': user.id}) }}">show</a>
                    <a href="{{ path('detail_edit', {'id': user.id}) }}">edit</a>
                    <a href="{{ path('detail_edit', {'id': user.id}) }}">add</a>
                {%  endif %}
                {%  endif %}

    </div>
</div>
</div>
<br><br><br><br><br><br><br><br>
{% endblock %}

